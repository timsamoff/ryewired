{
  "id": "led",
  "label": "LED",
  "category": "semiconductor",
  "symbol": "D",
  "description": "Light-emitting diode. Emits light proportional to current.",
  "legs": 2,
  "leg_span": 2,
  "polarized": true,
  "properties": [
    { "key": "color", "label": "Color", "type": "select", "options": ["Red","Green","Blue","Yellow","White","Orange","UV"], "default": "Red",
      "hint": "Sets the LED's actual turn-on voltage — different colors need different amounts of voltage to light up." },
    { "key": "forward_voltage", "label": "Forward Voltage (V)", "type": "number", "default": 2.0,
      "hint": "The voltage it takes to turn this LED on. Set automatically by Color — override only for a specific real part." },
    { "key": "max_current_ma", "label": "Max Current (mA)", "type": "number", "default": 20,
      "hint": "The most current this LED can safely handle. Always pair an LED with a series resistor to keep current below this." }
  ],
  "behavior": { "type": "led" },
  "failure_modes": {
    "over_current": { "threshold_multiplier": 1.5, "result": "burn", "message": "LED burned out from too much current. Always use a current-limiting resistor in series. R = (Vsupply - Vforward) / Imax" },
    "reverse_voltage": { "threshold_v": -5, "result": "silent_fail", "message": "Either the LED is installed backwards or the power supply polarity is reversed. The LED's anode (+) should be connected to the positive rail, and the cathode (–) to the negative rail. If the LED is oriented correctly, check that the + and – connections from the power supply haven't been swapped." }
  },
  "color_map": {
    "Red":    { "hex": "#ff2200", "vf": 2.0 },
    "Green":  { "hex": "#00cc44", "vf": 2.1 },
    "Blue":   { "hex": "#0044ff", "vf": 3.2 },
    "Yellow": { "hex": "#ffcc00", "vf": 2.1 },
    "White":  { "hex": "#ffffff", "vf": 3.3 },
    "Orange": { "hex": "#ff7700", "vf": 2.0 },
    "UV":     { "hex": "#8800ff", "vf": 3.4 }
  },
  "visual": {
    "body_width": 20,
    "body_height": 32,
    "lead_length": 10
  }
}