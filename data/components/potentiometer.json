{
  "id": "potentiometer",
  "labelKey": "component.potentiometer.label",
  "category": "passive",
  "symbol": "RV",
  "descriptionKey": "component.potentiometer.description",
  "legs": 3,
  "leg_span": 2,
  "properties": [
    {
      "key": "resistance",
      "labelKey": "component.potentiometer.resistance.label",
      "type": "value_unit",
      "default": 100000,
      "default_unit": "kΩ",
      "units": [
        {"label":"Ω","factor":1},
        {"label":"kΩ","factor":1e3},
        {"label":"MΩ","factor":1e6}
      ],
      "hintKey": "component.potentiometer.resistance.hint"
    },
    {
      "key": "taper",
      "labelKey": "component.potentiometer.taper.label",
      "type": "select",
      "options": [
        "Linear (B)",
        "Audio/Log (A)",
        "Reverse Audio (C)"
      ],
      "default": "Linear (B)",
      "hintKey": "component.potentiometer.taper.hint",
      "_i18nNote": "options are NOT translation keys — js/simulation.js and js/audio-engine.js match against the literal substring 'Audio' in this value at 6 call sites to decide the audio-taper (squared) response curve. Translating these strings would silently break that electrical behavior, not just the display. Needs a stable non-text taper identifier before this can be localized — out of scope for the current text-only i18n pass."
    },
    {
      "key": "wiper",
      "labelKey": "component.potentiometer.wiper.label",
      "type": "range",
      "min": 0,
      "max": 1,
      "step": 0.01,
      "default": 0.5,
      "live": true,
      "hintKey": "component.potentiometer.wiper.hint"
    }
  ],
  "behavior": {
    "type": "potentiometer"
  },
  "failure_modes": {
    "over_power": {
      "threshold_multiplier": 2.0,
      "result": "burn",
      "messageKey": "component.potentiometer.failure.over_power"
    }
  },
  "visual": {
    "body_color": "#555555",
    "body_width": 40,
    "body_height": 40,
    "lead_length": 8
  },
  "leg_labels": [
    "CCW",
    "Wiper",
    "CW"
  ]
}
