{
  "id": "switch_spst",
  "label": "Switch (SPST)",
  "category": "switch",
  "symbol": "SW",
  "description": "Single-pole single-throw switch. On/off.",
  "legs": 2,
  "leg_span": 2,
  "properties": [
    { "key": "type", "label": "Action", "type": "select", "options": ["Latching","Momentary (NO)","Momentary (NC)"], "default": "Latching",
      "hint": "How the switch behaves when clicked: Latching stays in the new position; Momentary springs back when released (Normally Open = off until held, Normally Closed = on until held)." },
    { "key": "state", "label": "Initial State", "type": "select", "options": ["Open","Closed"], "default": "Open",
      "hint": "Whether the switch starts open (circuit broken) or closed (circuit connected) when the board loads." }
  ],
  "behavior": { "type": "switch_spst" },
  "failure_modes": {},
  "visual": {
    "body_color": "#333333",
    "body_width": 32,
    "body_height": 18
  }
}
